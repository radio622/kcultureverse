# 🐛 QA 이슈 추적 — V7.4.1 (최종 정리 - 2026-03-23)

> **상태 요약**: 일부 UI 버그는 코드 수정을 완료했으나, 현재 Vercel 배포 시스템 오류(middleware 관련)로 인해 실 서버 반영이 막혀 있습니다. 다음 세션에서는 배포 장애 해결과 함께 예약된 UI 개선/기능 개발을 이어서 진행합니다.

---

## 🔴 다음 세션 이관 이슈 목록 (Pending Issues)

### #1 바텀시트(카드덱) UI 정렬 및 여백 이슈
- **현재 상태**: 높이 계산 방식과 쓸데없는 여백(padding/margin)을 1차적으로 줄이는 코드가 배포되었음.
- **문제점**: 화면에서 여전히 카드가 너무 작게 보이거나, 버튼 등이 어긋나 보이는 등 UI 레이아웃이 여전히 "엉망"으로 느껴지는 문제가 잔존.
- **다음 작업**: `BottomSheet.tsx`, `MiniPlayer.tsx`, `warp-list` CSS 등을 브라우저 개발자 도구로 정밀 수치 조절 및 반응형(vw, vh) 테스트 필요.

### #2 수동 미리듣기 이어재생 (자율주행 유사 기능)
- **현재 상태**: 기능 미구현 상태. 현재 클릭 시 30초만 재생 후 정지됨. (원래 자율주행 `isWarping` 전용 기능이었음)
- **문제점**: 사용자는 노래가 끝나면 다음 곡으로 넘어가는(또는 같은 아티스트의 다음 곡으로) 동작을 기대함.
- **다음 작업**: `MiniPlayer`와 오디오 재생 컨텍스트(`useAudio` 또는 백그라운드 관리 훅)를 연동하여 트랙 종료 시 다음 큐로 넘기는 로직 신규 구현.

### #3 엣지(관계선) 클릭 팝업 이슈 지속 확인
- **현재 상태**: 포커스 유무와 상관없이 클릭 가능하게 코드는 수정 및 배포됨.
- **문제점**: Vercel 배포 후에도 여전히 클릭 시 설명 팝업이 안 뜬다면, 이벤트 감지(pointerEvents)나 z-index 문제일 가능성 높음.
- **다음 작업**: `GraphCosmos.tsx`의 엣지 렌더 영역 및 `onLinkClick` 콜백 작동 여부를 콘솔 로그로 추적.

### #4 Vercel 반영 불가 (빌드 파이프라인 에러)
- **현재 상태**: 로컬 커밋은 성공했으나 Vercel 배포 파이프라인이 계속 깨져서 실제 `frompangyo.vercel.app`에 수정 코드가 업로드되지 않고 있음.
- **문제점**: Vercel 콘솔의 Error Logs 확인 결과 `ENOENT: no such file or directory, open '/vercel/path0/.next/server/middleware.js.nft.json'` 로그를 뿜으며 빌드가 완전히 중단됨.
- **다음 작업**: Next.js 14/15의 middleware 빌드 Output Tracing 설정, 혹은 Vercel 환경변수/캐시 이슈를 리셋하고 빌드 구성을 다시 세팅해야 함.

---

## ✅ 완료 및 반영된 이슈 (Closed Issues)
- **우주맵 아티스트 사진 안 나옴**: LOD 렌더링 조건 완화 및 `new Image()` onload 재렌더 이벤트 추가로 해결. (배포 완료)
- **로그인 아이콘 가림**: 우측 상단 공유버튼과 유저아바타의 flex 병합으로 해결. (배포 완료)
- **Vercel 배포 실패**: `OPENAI_API_KEY` 빌드시점 호출을 런타임 지연초기화(lazy initialization)로 수정하여 해결 완료.
- **iTunes 동명 곡 오작동 방어**: 로직 수정 반영.
- **줌버튼 UX 센터링 기능**: 반영 완료.

---

## 📂 관련 주요 파일
| 요소 | 파일 경로 |
|------|-----------|
| 바텀시트 UI | `src/components/BottomSheet.tsx`, `src/app/universe/page.tsx` (`warp-list` css) |
| 플레이어/오디오 | `src/components/MiniPlayer.tsx`, `src/hooks/useAutoWarp.ts` |
| 엣지/관계선 | `src/components/GraphCosmos.tsx` |
