# K-Culture Universe V5.5 — 공식 아키텍처 문서

> **버전**: V5.5 (Egalitarian Organic Network)
> **최종 수정**: 2026-03-21
> **상태**: 구현 진행 중

---

## 핵심 원칙

> 모든 아티스트는 우주의 평등한 별이다.
> 별의 크기와 밝기는 오직 `degree`(연결된 간선 수)에 비례하는
> 연속적(continuous) 스케일로만 자연 결정된다.
> 어떤 코드도 특정 아티스트를 강제로 크게 만들거나 작게 만들지 않는다.

---

## 데이터 수집 파이프라인

```
[Seed 아티스트 62명] → BFS Queue
  ├─ getArtistRelations()           → 그룹/멤버 관계
  ├─ getComprehensiveCredits()      → 전앨범 크레딧
  │    ├─ Release Group (앨범/EP/싱글) 최대 25개
  │    ├─ 각 앨범 → 트랙 → Recording rels (producer, vocal)
  │    └─ 각 트랙 → Work rels (★ composer, ★ lyricist)
  └─ getArtistDiscography()         → 발매일 데이터
       ↓
  발견된 아티스트를 Queue에 추가 → BFS 반복
```

### 관계 매핑
| MusicBrainz role | → V5 타입 | 화면 표시 | 색상 |
|---|---|---|---|
| member, group | SAME_GROUP | 그룹 멤버 | #86efac |
| composer, lyricist | WRITER | 작곡/작사 | #fbbf24 |
| producer | PRODUCER | 프로듀서 | #60a5fa |
| vocal, guest | FEATURED | 피처링 | #c084fc |
| 기타 | INDIRECT | 협업 | 반투명 |

---

## 타입 시스템

```typescript
interface V5Node {
  id: string;          // MusicBrainz ID
  name: string;
  nameKo: string;
  degree: number;      // ★ 연결 수 (연속 스케일)
  accent?: string;     // degree 기반 자동 색상
  // ... image, genres, popularity, x, y
}

interface V5Edge {
  source: string;
  target: string;
  weight: number;      // 0.1 ~ 1.0
  relation: string;    // SAME_GROUP | WRITER | PRODUCER | FEATURED | INDIRECT
  label?: string;      // 자동 생성: "그룹 멤버", "작곡/작사" 등
}
```

---

## 렌더링 (GraphCosmos.tsx)

### degree 연속 스케일 공식
| LOD | 별 크기(r) | 공식 |
|-----|-----------|------|
| FAR | 1~6px | `1.0 + √degree × 0.5` |
| MID | 3~23px | `3 + √degree × 2` |
| CLOSE | 10~50px | `10 + √degree × 4` |

### 조건부 효과
- **글로우**: degree ≥ 15 이상 또는 포커스/1촌
- **이름 표시**: degree ≥ 15 이상 (FAR에서), 포커스/1촌 (MID/CLOSE)
- **이미지 로드**: degree ≥ 15 이상 또는 포커스/1촌

---

## npm 스크립트

```bash
npm run v5:crawl-bfs    # BFS 크롤러 실행
npm run v5:build        # 좌표 베이킹 + JSON 3분할
```
